# ★构造函数
```javascript
function Star(uname,age){
  this.uname = uname;
  this.age = age;
}
var ldh = new Star('刘德华',19);
```
## 给构造函数添加方法

1. 直接在函数内部添加

问题：每生成一个实例都会在堆内开辟一个 存放方法的空间，（浪费内存）
```javascript
function Star(uname,age){
  this.uname = uname;
  this.age = age;
  this.sing = function(){
     console.log("唱歌");
  }
}
var ldh = new Star('刘德华',19);
```

2. 在构造函数的原型对象中添加

问题：添加多个方法给原型对象赋值一个对象的时候，相当于用对象覆盖掉原来原型对象里面的内容，（把指向构造函数的constructor覆盖掉），如果要使用对象赋值的方式给构造函数添加方法，需要在对象内部添加constructor：Star 让原型对象指向构造函数。
```javascript
function Star(uname,age){
  this.uname = uname;
  this.age = age;
}
Star.prototype.sing = function(){
   console.log("唱歌");
}
Star.prototype = {
      constructor: Star,

      sing: function () {
        console.log('我在唱歌');
      },
      movie: function () {
        console.log('我在电影');
      },
      dance: function () {
        console.log('我在唱歌');
      },
    }
var ldh = new Star('刘德华',19);
```
## 构造函数new的过程

1. 在内存创建一个新的空对象。
1. 让空对象的对象的原型指向构造函数的原型对象。（ldh.__proto__=Star.prototype）
1. 让this指向这个新对象。（apply（ldh，arguments））
1. 执行构造函数里面的代码，给新对象添加属性 。
1. 返回这个新对象。
# ★原型链
## 📝 原型链图
![](https://cdn.nlark.com/yuque/0/2022/jpeg/29071883/1653562904795-ea8885d7-e770-4119-91c1-146e4ed2f6e0.jpeg)

## 继承

1. call(obj，arg1,arg2)

obj：想让当前调用函数this指向的对象。
作用：①：调用函数
    ②：修改函数的this指向

2. 构造函数继承父类属性：call()
```javascript
function Father(uname,age){
  this.uname = name;
  this.age = age;
}
function Son(uname,age){
  Father.call(this,uname,age);//此时找个this指向的是Son的实例对象
}
var son = new Son('儿子',19);
```
call()让调用函数的this指向 想被指向的this（Son的this），让Father的this指向Son的this，只要Son传入参数就可以调用Father的属性。

3. 构造函数继承父类的方法 ：

①让 Son 的 prototype 指向 Father 的 prototype。
问题：
     	 会出现 Son 和 Father 的 prototype 指向同一个对象，
      	那么修改 Son 的 prototype 时，Father 的 prototype 也会变化。
因为找个操作让Son的原型对象和 Father的原型对象的地址指向了同一个内存
```javascript
function Father(uname, age) {
      // this 指向父构造函数的对象实例
      this.uname = uname;
      this.age = age;
    }

    // 2 .子构造函数
    function Son(uname, age, score) {
      // this 指向子构造函数的对象实例
      Father.call(this, uname, age);
      this.score = score;
    }
    Father.prototype.money = function () {
      console.log('父亲挣钱');
    }
    // 解决方案1：让 Son 的 prototype 指向 Father 的 prototype。
    // Son.prototype = Father.prototype;
    /*
      
    */
    Son.prototype.exam = function () {
      console.log('我要考试');
    }
    /*

    var son = new Son("刘德华", 18, 100);
```

②让 Son 的 prototype 指向 Father 的对象实例
问题：此时Son.prototype的construc属性会被覆盖，没有此属性 ，输入Son.prototype.constructor时指向的是Father里的constructor
解决：让Son.prototype.constructor指回Son。
     Son.prototype.constructor = Son;
```javascript
 function Father(uname, age) {
      // this 指向父构造函数的对象实例
      this.uname = uname;
      this.age = age;
    }

    // 2 .子构造函数
    function Son(uname, age, score) {
      // this 指向子构造函数的对象实例
      Father.call(this, uname, age);
      this.score = score;
    }
    Father.prototype.money = function () {
      console.log('父亲挣钱');
    }

    
     Son.prototype = new Father();
     Son.prototype.constructor = Son;
    Son.prototype.exam = function () {
      console.log('我要考试');
    }

    var son = new Son("刘德华", 18, 100);
```
## 📝 继承方法原型链
![](https://cdn.nlark.com/yuque/0/2022/jpeg/29071883/1653567137116-71e512b8-54b0-4ffd-9b01-bea50f1d3b1a.jpeg)




