## call()：可以修改函数this指向 ，且调用函数
call(想要指向的对象，参数1，参数2)
## apply():可以修改this指向，且调用函数
apply（想要指向的对象，数组或者是伪数组的参数）
## bind（）：可以修改this指向，但不调用，不修改原函数
bind（想要指向的对象，参数1，参数2），返回 原函数改变this之后的新函数

1. 相当于复制了一份新的函数，bind只在这个复制的函数上边 操作，返回的也是这个被复制的函数，原函数不变。
